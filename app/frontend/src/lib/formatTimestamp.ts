/** Short label for chart X-axis from an ISO timestamp. */
export function formatChartTimestamp(ts: string): string {
  const d = new Date(ts);
  return `${d.getMonth() + 1}/${d.getDate()} ${d.getHours()}:${String(d.getMinutes()).padStart(2, "0")}`;
}
